namespace UserDomainServer.Settings
{
    public enum EnvironmentTypes
    {
        Azure,
        OnPremises,
    }

    public enum EnvironmentSubtypes
    {
        SingleTenant,
        MultiTenant,
    }
}
